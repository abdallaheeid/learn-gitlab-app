stages:
  - build
  - test

build_website:
  image: node:22-alpine
  stage: build
  script:
    - echo "Starting build..."
    - node --version
    - npm --version
    - npm ci
    - npm run build
  artifacts:
    paths:
      - build/
  
